import DefaultLayout from "@/layouts/default";

export default function IndexPage() {
  return (
    <DefaultLayout>
      <div>
        {/* Empty content area */}
        {/* The Navbar is included in the DefaultLayout */}
      </div>
    </DefaultLayout>
  );
}